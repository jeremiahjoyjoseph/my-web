import React from "react";
import WeddingWebWrapper from "../../containers/weddingWebWrapper/weddingWebWrapper";

const Landing = (props) => {
  return (
    <WeddingWebWrapper>
      <div>Landing</div>
    </WeddingWebWrapper>
  );
};

export default Landing;
